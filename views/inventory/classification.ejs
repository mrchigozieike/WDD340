<!--This will show three distint sections:
 A Content heading, 
 a message and the Inventory Items Display. -->

<% if (title) { %>
  <h1>
    <%= title %>
  </h1>
  <% } else { res.redirect('/') } %>

    <%# messages() %>

      <%- grid %>